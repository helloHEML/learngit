package com.rongke.utils.ans;import java.io.UnsupportedEncodingException;import java.util.Random;public class StringEncodingUtil {    public static  String utf(String a){        String b = null;        try {             b = new String(a.getBytes("ISO-8859-1"),"utf-8");             b= b.replace("\\","");        } catch (UnsupportedEncodingException e) {            e.printStackTrace();        }        return b;    }    // 创建一个新的长整形编号并转为字符串类型    public static String createNewId() {        return new IdWorker(1, new Random().nextInt(31)).nextId() + "";    }    public static long createNewIdLONG() {        return new IdWorker(1, new Random().nextInt(31)).nextId();    }}