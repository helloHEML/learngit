package com.rongke.mapper;import com.baomidou.mybatisplus.mapper.Wrapper;import com.baomidou.mybatisplus.plugins.Page;import com.rongke.model.User;import com.baomidou.mybatisplus.mapper.BaseMapper;import org.apache.ibatis.annotations.Param;import org.apache.ibatis.session.RowBounds;import java.util.List;import java.util.Map;/** * @UserMapper * @用户Mapper * @version : Ver 1.0 */public interface UserMapper extends BaseMapper<User>{    List<User>  selectByCondition(User user);    List<User> findUserDateList(Map map);    Integer findUserDateListSize(Map map);    List<User> findChannelUserList(Map requestmap);    List<Map<String,Object>> userORChaORAuto(Map map);    /**     * 用户列表     */     List<User> userPage(RowBounds var1, @Param("ew") Wrapper<User> var2);}